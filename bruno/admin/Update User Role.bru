meta {
  name: Update User Role
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/admin/users/{{userId}}/role
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "role": "super"
  }
}
